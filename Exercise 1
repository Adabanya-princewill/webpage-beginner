1. A. Photoshop page sketch
   B. Site diagram
   C. (X)HTML and CSS documents
   D. PHP scripts

2. world wide web consortium

3. A. identifies text as a second-level heading
   B. Makes all second-level headings blue
   C. Creates a custom server-side web application
   D. Checks a form field for a valid entry
   E. Defines a new markup language for sharing
      financial information

4. Front end development is programming which focuses on the visual elements of a website or app that a user will interact with (the client side).
    Back end development focuses on the side of a website users can't see (the server side).

5. What is the difference between a web-authoring program and an HTML-editing tool? A web-authoring tool provides a visual interface for creating entire web pages, including the necessary HTML, CSS, and scripts. HTML editors provide
   only shortcuts to writing HTML documents manually.

